/**
 * 复合语句：
 */

{
    x = Math.PI;
    cx = Math.cos(x);
    console.log("cos(x)=" + cx);
}

/**
 * 空语句： 执行空语句的时候什么都不用做，只是为了正常执行；
 */
//执行for循环 这种就是把循环放到了更新进行了，所以循环体内不需要执行操作了；
for (let i = 0; i < a.length; a[i++] = 0);

